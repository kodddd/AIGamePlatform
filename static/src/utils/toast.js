// import { useToast } from "@chakra-ui/react";

// export const useAppToast = () => {
//   const toast = useToast();

//   return {
//     success: (message, description) =>
//       toast({ title: message, description, status: "success", ...baseConfig }),

//     error: (message, description) =>
//       toast({ title: message, description, status: "error", ...baseConfig }),

//     info: (message) => toast({ title: message, status: "info", ...baseConfig }),
//   };
// };

// const baseConfig = {
//   duration: 5000,
//   isClosable: true,
//   position: "top-right",
// };
